<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.iu33.crudfirebase">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".activity.RegisterActivity" />
        <activity android:name=".activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.LoginPhoneActivity" />
        <activity android:name=".activity.LoginGoogleActivity" />
        <activity
            android:name=".activity.DatabaseFirebaseActivity"
            android:label="@string/title_activity_database_firebase"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".admob.AdmobActivity"/>
        <activity android:name=".admob.BannerActivity"/>
        <activity android:name=".admob.IntersitialActivity"/>
        <activity android:name=".admob.RewardVideoActivity"/>
    </application>

</manifest>